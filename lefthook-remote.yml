---
pre-commit:
  commands:
    shellcheck:
      glob: "*.{sh,bats}"
      run: timeout ${LEFTHOOK_SHELLCHECK_TIMEOUT:-30} shellcheck {staged_files}

pre-push:
  commands:
    shellcheck:
      glob: "*.{sh,bats}"
      run: timeout ${LEFTHOOK_SHELLCHECK_TIMEOUT:-30} shellcheck {push_files}
